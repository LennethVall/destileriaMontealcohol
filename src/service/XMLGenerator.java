/* Alvaro */

package service;

// Interfaz que define el contrato para la generacion del archivo XML del sistema
public interface XMLGenerator {

    // Genera el archivo XML con todos los datos del sistema (clientes, proveedores, productos y pedidos)
    void generarXML() throws Exception;
}

/* Alvaro */
